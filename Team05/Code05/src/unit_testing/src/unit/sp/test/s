//
// Created by sjh_9 on 23/2/2024.
//
Procedure {
  t + 1 = y + 3 * (1 + y);
  call hello;
  read t;
  print u;
}



Procedure {
  x = y + 3;
  call hello;
  read t;
  print u;
  if (x == 1) then {
    t + 1 = y + 3 * (1 + y);
    call hello;
    read t;
    print u;
  } else {
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}

Procedure {
  x = y + 3;
  call hello;
  read t;
  print u;
  if (x == 1) then {
    x = y + 3;
    call hello;
    read t;
    print u;
  } else {
    x = y + 3;
    call hello;
    read t;
    print u;
  }
  x = y + 3;
  call hello;
  read t;
  print u;
}

Procedure {
  if (x == 1) then {
    x = y + 3;
    call hello;
    read t;
    print u;
  } else {
    x = y + 3;
    call hello;
    read t;
    print u;
  }
  x = y + 3;
  call hello;
  read t;
  print u;
}

Procedure {
  x = y + 3;
  call hello;
  read t;
  print u;
  while (x == 1) {
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}

Procedure {
  x = y + 3;
  call hello;
  read t;
  print u;
  while (x == 1) {
    x = y + 3;
    call hello;
    read t;
    print u;
  }
  x = y + 3;
  call hello;
  read t;
  print u;
}

Procedure {
  while (x == 1) {
    x = y + 3;
    call hello;
    read t;
    print u;
  }
  x = y + 3;
  call hello;
  read t;
  print u;
}

Procedure {
  if (x == 1) then {
    x = y + 3;
    call hello;
    read t;
    print u;
    if (x == 1) then {
      t + 1 = y + 3 * (1 + y);
      call hello;
      read t;
      print u;
    } else {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
  } else {
    x = y + 3;
    call hello;
    read t;
    print u;
    if (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    } else {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
  }
}


Procedure {
  if (x == 1) then {
    x = y + 3;
    call hello;
    read t;
    print u;
    if (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    } else {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  } else {
    x = y + 3;
    call hello;
    read t;
    print u;
    if (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    } else {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}

Procedure {
  if (x == 1) then {
    if (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    } else {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  } else {
    if (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    } else {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}

Procedure {
  if (x == 1) then {
    x = y + 3;
    call hello;
    read t;
    print u;
    while (x == 1) {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
  } else {
    x = y + 3;
    call hello;
    read t;
    print u;
    while (x == 1) {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
  }
}

Procedure {
  if (x == 1) then {
    x = y + 3;
    call hello;
    read t;
    print u;
    while (x == 1) {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  } else {
    x = y + 3;
    call hello;
    read t;
    print u;
    while (x == 1) {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}

Procedure {
  if (x == 1) then {
    while (x == 1) {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  } else {
    while (x == 1) {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}

Procedure {
  while (x == 1) {
    x = y + 3;
    call hello;
    read t;
    print u;
    if (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    } else {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
  }
}

Procedure {
  while (x == 1) {
    x = y + 3;
    call hello;
    read t;
    print u;
    if (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    } else {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}

Procedure {
  while (x == 1) {
    if (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    } else {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}

Procedure {
  while (x == 1) {
    x = y + 3;
    call hello;
    read t;
    print u;
    while (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
  }
}

Procedure {
  while (x == 1) {
    x = y + 3;
    call hello;
    read t;
    print u;
    while (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}

Procedure {
  while (x == 1) {
    while (x == 1) then {
      x = y + 3;
      call hello;
      read t;
      print u;
    }
    x = y + 3;
    call hello;
    read t;
    print u;
  }
}